name: Deploy Pipeline (ACR & OSS)

on:
  # 🔴 改为 Release 触发 - 使用 Tag 作为版本号
  release:
    types: [published]
  
  # 保留手动触发（用于测试）
  workflow_dispatch:
    inputs:
      version_tag:
        description: '版本标签 (如: v1.0.0)'
        required: true
        default: 'v0.0.0-test'

# 🔴 全局配置
env:
  # ACR 配置 (杭州)
  REGISTRY: ${{ secrets.ACR_REGISTRY }}
  NAMESPACE: capgemini-poc
  IMAGE_NAME: pai-poc
  
  # OSS 配置 (上海)
  OSS_ENDPOINT: ${{ secrets.OSS_ENDPOINT }}
  OSS_BUCKET: ${{ secrets.OSS_BUCKET }}

permissions:
  contents: read
  security-events: write

jobs:
  # =========================================================
  # 任务 0: 版本号解析
  # =========================================================
  prepare:
    name: 📋 Prepare Version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.tag }}
      # 移除 image_uri 输出，因为它包含 secret 会被 GitHub 跳过
    steps:
      - name: Extract Version Tag
        id: version
        run: |
          # 根据不同触发方式获取版本号
          if [ "${{ github.event_name }}" == "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
          elif [ "${{ github.event_name }}" == "push" ]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          else
            VERSION="${{ github.event.inputs.version_tag }}"
          fi
          
          if [ -z "$VERSION" ]; then
            echo "❌ 错误: 版本号为空！"
            echo "触发方式: ${{ github.event_name }}"
            exit 1
          fi
          
          echo "tag=${VERSION}" >> $GITHUB_OUTPUT
          echo "📦 版本号: ${VERSION}"

  # =========================================================
  # 任务 1: 安全扫描 (CodeQL)
  # =========================================================
  analyze-security:
    name: 🛡️ Security Gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: 'python'
      - uses: github/codeql-action/autobuild@v3
      - uses: github/codeql-action/analyze@v3

  # =========================================================
  # 任务 2: 构建并推送 Docker 镜像
  # =========================================================
  build-and-push-acr:
    name: 🐳 Build & Push to ACR
    needs: [prepare, analyze-security]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and Push
        env:
          REGISTRY: ${{ secrets.ACR_REGISTRY }}
          NAMESPACE: capgemini-poc
          IMAGE_NAME: pai-poc
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          IMAGE_URI="${REGISTRY}/${NAMESPACE}/${IMAGE_NAME}:${VERSION}"
          
          echo "🔨 构建镜像: ${IMAGE_URI}"
          docker build -t "${IMAGE_URI}" .
          
          echo "📤 推送镜像..."
          docker push "${IMAGE_URI}"
          
          echo "✅ 镜像推送成功: ${IMAGE_URI}"

      - name: Verify Image Push
        env:
          REGISTRY: ${{ secrets.ACR_REGISTRY }}
          NAMESPACE: capgemini-poc
          IMAGE_NAME: pai-poc
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          IMAGE_URI="${REGISTRY}/${NAMESPACE}/${IMAGE_NAME}:${VERSION}"
          
          echo "🔍 验证镜像推送状态..."
          if docker manifest inspect "${IMAGE_URI}" > /dev/null 2>&1; then
            echo "✅ 镜像验证成功: ${IMAGE_URI}"
          else
            echo "❌ 镜像验证失败，可能推送未完成"
            exit 1
          fi

  # =========================================================
  # 任务 3: 上传代码到 OSS (优化版)
  # =========================================================
  upload-to-oss:
    name: ☁️ Upload to OSS
    needs: [prepare, analyze-security]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup OSS Tool
        uses: manyuanrong/setup-ossutil@v2.0
        with:
          endpoint: "${{ env.OSS_ENDPOINT }}"
          access-key-id: "${{ secrets.ALIYUN_ACCESS_KEY_ID }}"
          access-key-secret: "${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}"

      # 🆕 优化: 只上传必要文件
      - name: Prepare Upload Files
        env:
          REGISTRY: ${{ secrets.ACR_REGISTRY }}
          NAMESPACE: capgemini-poc
          IMAGE_NAME: pai-poc
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          IMAGE_URI="${REGISTRY}/${NAMESPACE}/${IMAGE_NAME}:${VERSION}"
          
          echo "📁 准备上传文件..."
          
          mkdir -p .upload_staging
          
          cp -r main.py .upload_staging/ 2>/dev/null || true
          cp -r Dockerfile .upload_staging/ 2>/dev/null || true
          cp -r requirements.txt .upload_staging/ 2>/dev/null || true
          cp -r src/ .upload_staging/ 2>/dev/null || true
          cp -r app/ .upload_staging/ 2>/dev/null || true
          cp -r config/ .upload_staging/ 2>/dev/null || true
          
          cat > .upload_staging/manifest.json << EOF
          {
            "version": "${VERSION}",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "commit_sha": "${{ github.sha }}",
            "release_url": "${{ github.event.release.html_url }}",
            "image_uri": "${IMAGE_URI}"
          }
          EOF
          
          echo "📋 待上传文件列表:"
          ls -la .upload_staging/

      - name: Upload to OSS
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          OSS_PATH="oss://${{ env.OSS_BUCKET }}/releases/${VERSION}/"
          
          echo "📤 上传到: ${OSS_PATH}"
          
          # 上传文件
          ossutil cp -r .upload_staging/ "${OSS_PATH}" --include "*"
          
          echo "✅ 代码上传成功"

      # 🆕 更新 latest 指针文件
      - name: Update Latest Pointer
        env:
          REGISTRY: ${{ secrets.ACR_REGISTRY }}
          NAMESPACE: capgemini-poc
          IMAGE_NAME: pai-poc
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          IMAGE_URI="${REGISTRY}/${NAMESPACE}/${IMAGE_NAME}:${VERSION}"
          
          echo "${VERSION}" > latest.txt
          ossutil cp latest.txt "oss://${{ env.OSS_BUCKET }}/releases/latest.txt" -f
          
          cat > latest.json << EOF
          {
            "version": "${VERSION}",
            "updated_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "image_uri": "${IMAGE_URI}",
            "oss_path": "releases/${VERSION}/"
          }
          EOF
          ossutil cp latest.json "oss://${{ env.OSS_BUCKET }}/releases/latest.json" -f
          
          echo "✅ Latest 指针更新完成"

      # 🆕 部署状态检查 - 验证 OSS 上传
      - name: Verify OSS Upload
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          
          echo "🔍 验证 OSS 上传状态..."
          
          # 列出上传的文件
          ossutil ls "oss://${{ env.OSS_BUCKET }}/releases/${VERSION}/" --limited-num 20
          
          # 检查 manifest.json 是否存在
          if ossutil stat "oss://${{ env.OSS_BUCKET }}/releases/${VERSION}/manifest.json" > /dev/null 2>&1; then
            echo "✅ OSS 上传验证成功"
          else
            echo "❌ OSS 上传验证失败"
            exit 1
          fi

  # =========================================================
  # 任务 4: 更新 PAI EAS 服务
  # =========================================================
  update-pai-service:
    name: 🚀 Update PAI EAS Service
    needs: [prepare, build-and-push-acr]
    runs-on: ubuntu-latest
    if: needs.build-and-push-acr.result == 'success'
    steps:
      - name: Download eascmd64
        run: |
          wget -q https://eas-data.oss-cn-shanghai.aliyuncs.com/tools/eascmd64
          chmod +x eascmd64
          echo "✅ eascmd64 下载完成"

      - name: Configure eascmd64
        env:
          ALIYUN_AK: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
          ALIYUN_SK: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
        run: |
          ./eascmd64 config \
            -i "${ALIYUN_AK}" \
            -k "${ALIYUN_SK}" \
            -e "pai-eas.cn-hangzhou.aliyuncs.com"
          echo "✅ eascmd64 认证配置完成"

      - name: Prepare Update Config
        env:
          # 使用 VPC 内网地址，PAI 通过内网拉取镜像
          ACR_REGISTRY_VPC: crpi-gse517a1fwz5v90b-vpc.cn-hangzhou.personal.cr.aliyuncs.com
          NAMESPACE: capgemini-poc
          IMAGE_NAME: pai-poc
          VERSION: ${{ needs.prepare.outputs.version }}
          ACR_USERNAME: ${{ secrets.ACR_USERNAME }}
          ACR_PASSWORD: ${{ secrets.ACR_PASSWORD }}
        run: |
          # 构建 VPC 内网镜像地址（PAI 拉取用）
          IMAGE_URI="${ACR_REGISTRY_VPC}/${NAMESPACE}/${IMAGE_NAME}:${VERSION}"
          
          # 生成 dockerAuth (Base64 编码)
          DOCKER_AUTH=$(echo -n "${ACR_USERNAME}:${ACR_PASSWORD}" | base64 -w 0)
          
          # 创建更新配置文件（保持与现有配置一致）
          cat > update_service.json << EOF
          {
            "containers": [
              {
                "image": "${IMAGE_URI}",
                "port": 8000,
                "script": "python app.py"
              }
            ],
            "dockerAuth": "${DOCKER_AUTH}"
          }
          EOF
          
          echo "📄 更新配置:"
          cat update_service.json

      - name: Update PAI EAS Service
        run: |
          echo "🚀 开始更新 PAI EAS 服务: pai_poc"
          
          ./eascmd64 modify pai_poc -s update_service.json -w 556368
          
          echo "✅ PAI EAS 服务更新命令已执行"

      - name: Wait and Verify Service Status
        run: |
          echo "⏳ 等待服务更新 (30秒)..."
          sleep 30
          
          echo "🔍 检查服务状态:"
          ./eascmd64 desc pai_poc -w 556368
          
          echo ""
          echo "📊 服务版本信息:"
          ./eascmd64 desc pai_poc -w 556368 | grep -E "(image|version|status)" || true

  # =========================================================
  # 任务 5: 部署状态汇总
  # =========================================================
  deployment-summary:
    name: 📊 Deployment Summary
    needs: [prepare, build-and-push-acr, upload-to-oss, update-pai-service]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Generate Summary
        env:
          REGISTRY: ${{ secrets.ACR_REGISTRY }}
          NAMESPACE: capgemini-poc
          IMAGE_NAME: pai-poc
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          IMAGE_URI="${REGISTRY}/${NAMESPACE}/${IMAGE_NAME}:${VERSION}"
          
          echo "# 🚀 部署状态汇总" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 版本信息" >> $GITHUB_STEP_SUMMARY
          echo "| 项目 | 值 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| **版本号** | \`${VERSION}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit SHA** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **触发方式** | ${{ github.event_name }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## 部署产物" >> $GITHUB_STEP_SUMMARY
          echo "| 产物 | 地址 | 状态 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|------|" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.build-and-push-acr.result }}" == "success" ]; then
            echo "| 🐳 Docker 镜像 | \`${IMAGE_URI}\` | ✅ 成功 |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| 🐳 Docker 镜像 | \`${IMAGE_URI}\` | ❌ 失败 |" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ needs.upload-to-oss.result }}" == "success" ]; then
            echo "| ☁️ OSS 代码包 | \`releases/${VERSION}/\` | ✅ 成功 |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ☁️ OSS 代码包 | \`releases/${VERSION}/\` | ❌ 失败 |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # PAI EAS 状态
          if [ "${{ needs.update-pai-service.result }}" == "success" ]; then
            echo "| 🤖 PAI EAS 服务 | \`pai_poc\` | ✅ 成功 |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| 🤖 PAI EAS 服务 | \`pai_poc\` | ❌ 失败 |" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 下一步操作" >> $GITHUB_STEP_SUMMARY
          echo "- DataWorks 可通过读取 \`releases/latest.txt\` 获取最新版本" >> $GITHUB_STEP_SUMMARY

      - name: Check Overall Status
        if: |
          needs.build-and-push-acr.result != 'success' || 
          needs.upload-to-oss.result != 'success' ||
          needs.update-pai-service.result != 'success'
        run: |
          echo "❌ 部署存在失败项，请检查日志"
          exit 1